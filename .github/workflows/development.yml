name: Development

on: push

jobs:
  test:
    name: Test whether all files compile
    runs-on: ubuntu-latest
    container:
      image: ghcr.io/typst/typst:latest
    steps:
      - name: "checkout repository"
        uses: actions/checkout@v3

      - name: "test SarfTable"
        run: typst compile tests/SarfTableTest.typ --root .
